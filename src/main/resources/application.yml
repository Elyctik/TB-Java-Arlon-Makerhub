spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/makerhub
    username: pgadmin
    password: pgadmin
    driverClassName: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format-sql: true